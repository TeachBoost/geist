<?php   $this->partial( 'partials/menu' ); ?>

<header class="site-head">
    <div class="bg-image" id="parallax"></div>
    <div class="vertical overlay">
        <div class="site-head-content inner">
            <a class="mobile-burger" id="mobile-burger" href="javascript:;" data-state="off">
                <i class="fa fa-bars"></i>
            </a>
            <a class="blog-logo" href="<?php echo $this->url->get( '' ); ?>">
                <img src="<?php echo $this->url->get( 'img/square.png' ); ?>" alt="Blog Logo" />
            </a>
            <h1 class="blog-title">TeachBoost LaunchPad</h1>
            <h2 class="blog-description sans">
                <span>Teacher development and coaching redefined.</span>
                <span>Get TeachBoost for your school or district at <a href="http://teachboost.com">teachboost.com</a>.</span>
            </h2>
        </div>
    </div>
</header>

<main class="home content" role="main">
    <section class="column">

<?php   $this->partial( 'partials/sideboard' );

        foreach ( $posts as $post ):
            if ( str_eq( $post->status, "published" ) ):
                $categoryIcon = $post->getCategoryIcon(); ?>

        <article class="preview">
            <div class="preview-icon <?php echo $categoryIcon->color ?> circle">
                <i class="fa <?php echo $categoryIcon->class ?>"></i>
            </div>
            <header class="post-header">
                <h2 class="post-title">
                    <a href="<?php echo $post->getPath(); ?>">
                        <?php echo $post->title; ?>
                    </a>
                </h2>
                <span class="post-meta">
                    <?php echo date( 'l, F jS, Y', strtotime( $post->post_date ) ); ?>
                </span>
            </header>
            <section class="post-excerpt">
                <p>
                    <?php echo $post->excerpt; ?>
                </p>
            </section>
        </article>

<?php       endif;
        endforeach;

        // check if there are paginated results
        if ( $postCount > 10 ): ?>

        <div class="centered-text padding-top-20 padding-bottom-20">
            <a href="<?php echo $this->url->get( 'posts/page/2' ); ?>" class="fontsize-18 gray button">
                View Older Articles
                <i class="fa fa-long-arrow-right"></i>
            </a>
        </div>

<?php   endif; ?>

    </section>
</main>